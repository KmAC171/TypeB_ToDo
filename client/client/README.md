# TODO App — Frontend

React + Vite frontend.

## Setup

```bash
npm install
npm run dev
```

Runs on [http://localhost:5173](http://localhost:5173).
Make sure the backend is running on port 5000 first.

## Project Structure